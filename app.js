const express = require('express')
const app = express()
const port = 8000

app.get('/', (req, res) => {
  res.send('Hello World!')
})

app.listen(port, () => {
  console.log(`Example app listening on port ${port}`)
})













const styles 
{
    nombreDeClase: {
        backgroundColor: "#FFF"
    },
    nombreDeClaseDos: {
        color: "#000"
    }
}


View,
Text,
TextInput,
TouchableOpacity